{% extends "FrontendBundle::layout.html.twig" %}

{% block content %}
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.city) }}
        {{ form_row(form.address) }}
        {{ form_row(form.telephone) }}
        {{ form_row(form.annotation) }}
        {{ form_row(form.button) }}
        {{ form_row(form._token) }}
    {{ form_end(form) }}
    <a href="{{ path('frontend_cart_page') }}">
        <button class="btn btn-primary"><i class="glyphicon glyphicon-arrow-left"></i>Вернуться в корзину</button>
    </a>
{% endblock %}
